import Card from "./Card"

class SummonDigimonCard extends Card {
    constructor(id, name, type, digimonsCards) {
      super(id, name, type,);
      this.digimonsCards = digimonsCards;
    }
  }

export default SummonDigimonCard